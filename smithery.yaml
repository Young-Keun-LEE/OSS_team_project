name: mju-notice-mcp
version: 1.0.0
kind: mcp

entry: mju_crawler/mcp.json

build:
  files:
    - mju_crawler/**
    - requirements.txt
  install:
    - pip install --upgrade pip
    - pip install -r requirements.txt

  command: python mju_crawler/main.py